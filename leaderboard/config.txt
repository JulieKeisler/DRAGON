# Runtime config overrides for leaderboard/main.py
# Format: KEY = VALUE
# Supported keys:
# - Flat key (preferred): TARGETS = ndvi,savi
# - Class attribute:      Experiment.TARGETS = ndvi,savi
# - Path override:        Paths.EXTERNAL_DATA_CSV = /path/to/your.csv
# - Method override:      DRAGON_METHODS[index].field = value
# - Method field shortcut: loss_mode = full (applies to DRAGON_METHODS[0])

# provide one or many targets (comma-separated).
TARGETS = ndvi
# choose the number of runs for each targets and each run's strategy
N_RUNS = 1 
INIT_STRATEGIES = diverse
# Optional global tuning
N_TOP_FEATURES = 10
# you can choose to add noise such that y_noise = y + N(0, std(y) * NOISE_STD)
NOISE_STD = 0
N_ITERATIONS = 7000
K_INIT = 30
MAX_COMPLEXITY = 35
T_PER_LEVEL = 100

# Method-level overrides
description = DragonSR custom runtime on external CSV
parallel_mode = smart
loss_mode = full
var_aug = true
add_noise = true
 # you can choose the denoiser: lingam, gpr
denoiser = None
sampling = true
mc_dropout = false
