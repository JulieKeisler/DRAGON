# provide one target
TARGETS = y
# choose the number of runs for each targets and each run's strategy
N_RUNS = 10
# random or diverse
INIT_STRATEGIES = random
# number off kept top features
N_TOP_FEATURES = 10
# add noise such that y_noise = y + N(0, std(y) * NOISE_STD)
NOISE_STD = 0
N_ITERATIONS = 10000
K_INIT = 30
MAX_COMPLEXITY = 55
T_PER_LEVEL = 20
LOSS_THRESHOLD = None
SEARCH_LOSS = mse

Loss.COMPOSITION_PENALTY = 0.01
Loss.COMPOSITION_BASE = 2.0


Dragon.SPAR_OP_GROUPS = {
    "all": ["select", "unary", "power", "ln", "exp", "sin", "cos", "const"],
    "alg": ["select", "unary", "power", "const"],
    "alg_trig": ["select", "unary", "power", "sin", "cos", "const"],
    "alg_explog": ["select", "unary", "power", "ln", "exp", "const"],
    "all_no_const": ["select", "unary", "power", "ln", "exp", "sin", "cos"],
    "alg_no_const": ["select", "unary", "power"],
    "alg_trig_no_const": ["select", "unary", "power", "sin", "cos"],
    "alg_explog_no_const": ["select", "unary", "power", "ln", "exp"],
}


# Method-level overrides
description = DragonSR custom runtime on external CSV
parallel_mode = smart
loss_mode = channel
var_aug = true
add_noise = false
 # you can choose the denoiser: lingam, gpr
denoiser = None
sampling = true
mc_dropout = false
