"""DRAGON utility modules.

Submodules
----------
- ``symbolic/`` — Formula extraction, formatting, denoising, feature selection,
  loss functions, and OLS pipeline
- ``plot_functions`` — DAG text/SVG plotting (depends on ``symbolic``)
"""
